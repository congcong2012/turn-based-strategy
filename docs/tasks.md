# 里程碑与任务

> 规则：每个里程碑必须**可运行、可测试**，并同步更新 docs/ 下的文档。

## M0 · 设计冻结 ✅

- [x] 10 个游戏设计问题（题材：古代；地图：20×20+）
- [x] MVP GDD：docs/gdd.md（核心循环 / 回合状态机 / 地图单位资源战斗 / 联机与房间 / 胜负 / 明确不做）

## M1 · 可联机大厅（GitHub Pages 静态） ✅

交付物
- [x] Vite + React + TS 工程，可 `pnpm build` 出纯静态 `dist/`
- [x] Trystero（@trystero-p2p/mqtt）P2P 房间，公共信令默认 MQTT
- [x] 页面：昵称、房间码（归一化 + 随机生成）、加入、玩家列表、准备、开始（提示）
- [x] 第一个加入者自动成为房主，UI 显示👑房主标识
- [x] 加入/离开实时更新列表；准备状态经房主权威同步
- [x] 刷新后自动回到原房间（sessionStorage 记忆 + playerId 持久化），不产生重复玩家
- [x] 手动 SDP 降级入口（占位）+ 真实降级手段：信令策略切换 MQTT ↔ Torrent
- [x] GitHub Pages 子路径构建（base 相对路径 + Actions 注入）+ 部署工作流
- [x] 文档：docs/architecture.md、docs/protocol.md、docs/tasks.md

验收结果（本地实跑）

| 验收项 | 结果 |
| --- | --- |
| 两个浏览器同一网址 + 同一房间码 → 互相看到昵称 | ✅ `local.spec.ts` + `p2p.spec.ts`（真实 P2P，两个独立浏览器上下文） |
| 刷新页面后能重新加入房间 | ✅ 两条轨道均通过（真实 P2P 下 5 秒内恢复） |
| 房主标识正确 | ✅ 第一个加入者带👑，且客户端不显示 |
| 构建产物可部署到 GitHub Pages | ✅ `base: './'` 相对路径 + `preview.spec.ts` 无 404、无控制台报错 |
| 单元测试 | ✅ 33 passed |
| E2E | ✅ 10 passed（local 6 / p2p 1 / preview 3） |

## M2 · 游戏内核 + 棋盘渲染 + 部署阶段 ✅

交付物
- [x] 数据驱动 JSON：`src/data/{units,terrain,buildings,matchup,rules}.json` + `maps/ancient_01.json`（24×24 古道渡口，脚本生成）
- [x] 纯函数规则内核：Dijkstra 移动、确定性伤害与反击、占领、经济（收入/生产/维修）、回合状态机、胜负与计分
- [x] PixiJS 8 棋盘：地形/据点/单位/血条、可达与可攻击高亮、占领进度条、拖拽平移 + 滚轮缩放（0.5–2.0）
- [x] 部署阶段：预算 3000 / 上限 4 单位 / 部署区校验 / 双方确认后进入行动阶段
- [x] 房主权威指令协议：`cmd` → 校验 → `game` 全量广播；`cmdRejected` 回执；重连补发完整状态
- [x] 对局界面：回合/资金/据点统计、单位面板（占领/待机）、兵营生产面板、结束回合、结算遮罩

验收结果

| 项 | 结果 |
| --- | --- |
| 单元测试 | ✅ 74 passed（含规则内核 40+：移动/伤害/反击/占领/经济/回合/胜负） |
| E2E（本地传输，真实 canvas 点击） | ✅ `game.spec.ts`：大厅 → 部署（含越区被拒）→ 行动 → 双端同步 → 结束回合 → 对手行动 |
| E2E（生产） | ✅ 兵营生产 → 下一回合在兵营格出场 |
| 全量回归 | ✅ 12 E2E（local 8 / p2p 1 / preview 3） |

已知待办（M3）
- [ ] 增量补丁同步（当前为全量广播，状态变大后再优化）
- [ ] PixiJS 代码分割（主包已超过 500KB 提示阈值）
- [ ] 战斗动画/事件日志面板（目前只有状态变化）
- [ ] 地形/单位在移动路径上的逐格动画

## M3 · 断线重连 + 对局体验 ✅

交付物
- [x] **断线重连**：房主侧对局持久化（localStorage）、房主刷新后恢复整局并补发状态、对手掉线保留席位、轮到掉线玩家时可跳过其回合、房主掉线时客户端冻结等待
- [x] 中文战报面板（`GameEvent` → 文案为纯函数，主客两端各自渲染同一条事件流）
- [x] 移动补间动画（220ms）与受击闪光（380ms）
- [x] PixiJS 动态 import 分包：主包 530KB → 281KB（gzip 88KB）
- [x] 投降 UI（认输）+ 结算界面（胜/败/和局 + 返回大厅）

验收结果

| 项 | 结果 |
| --- | --- |
| 单元测试 | ✅ 82 passed（新增断线重连 4 条、战报文案 4 条） |
| E2E 断线重连 | ✅ 房主刷新→客户端暂停→房主回来恢复整局；对手刷新→席位保留→继续；对手关页面→房主跳过其回合 |
| E2E 对局闭环 | ✅ 部署/行动/结束回合、占领易主+战报同步、投降结算、兵营生产 |
| 全量回归 | ✅ 17 E2E（local 13 / p2p 1 / preview 3） |

## M4 · 经济平衡修订 ✅（进行中）

问题（玩家反馈）：**军费增长过快，后期花不完。**

量化（`tests/unit/balance.test.ts` 用真实 `src/data/*.json` 跑贪心花钱模拟）：

| 配置 | 满编回合 | 30 回合期末闲置资金 |
| --- | --- | --- |
| 旧：HQ 2000 / 兵营 500 / 村落 500 · 产能 1 · 上限 20 | 第 13 回合 | **104,200** |
| 仅降产出（HQ 1200 / 兵营 300 / 村落 400） | 第 18 回合 | 51,200 |
| 仅降产出 + 提产能（上限仍 20） | 第 18 回合 | 51,200（**提产能无效**） |
| **新：降产出 + 产能 2 + 上限 32** | **第 28 回合** | **9,200** |

结论：**瓶颈不是产能，而是"单位上限打满后没有任何消费出口"**——只降产出或只提产能都治不好。

已落地改动
- [x] 收入下调：王城 2000→1200、兵营 500→300、村落 500→400（基础收入 3000→1800/回合）
- [x] 产能提升：每座兵营每回合最多 2 单；出场位 = 兵营格 → 相邻空格 → 顺延
- [x] 上限放宽：每方 20 → 32
- [x] 收入梯度：兵营 < 村落 < 王城，村落成为真正的争夺目标
- [x] 平衡回归测试：把上述结论固化成断言（含旧配置基线对照）
- [x] 旧存档兼容：字段不全的对局自动丢弃，避免用错规则继续下棋

验收
| 项 | 结果 |
| --- | --- |
| 单元测试 | ✅ 88 passed（新增产能/出场位 3 条、平衡 3 条） |
| 模拟报告 | ✅ `pnpm exec vitest run tests/unit/balance.test.ts` 直接打印资金曲线 |

## M5 · 打磨与发布（待办）

- [ ] 增量补丁同步（当前全量广播；2 人局状态仅几 KB，暂不构成瓶颈）
- [ ] 移动路径逐格动画、战斗结算演出
- [ ] 移动端触屏打磨（长按查看单位、双指缩放）
- [ ] 音效占位
- [ ] 房间满员放开到 2–4 人可选（P2P 星型拓扑，房主权威）
- [ ] 观战/回放（数据已预留：种子 + 完整状态）
- [ ] 好友局实测后再回填数值（本轮的模拟只是模型，真实体感以实测为准）

## 常用命令

```bash
pnpm install          # 安装依赖
pnpm dev              # 本地开发（http://127.0.0.1:5173）
pnpm test             # 单元测试（33 个）
pnpm build            # 类型检查 + 生产构建（dist/）
pnpm preview          # 预览生产构建（http://127.0.0.1:4173）
pnpm e2e:local        # 本地传输 E2E（无需网络）
pnpm e2e:p2p          # 真实 P2P E2E（需要公网信令）
pnpm e2e              # 全部 E2E（含生产构建验收）
```

> 首次运行 E2E 需要 `pnpm exec playwright install chromium`（约 170MB）。
> CI 只跑单元测试 + 构建；E2E 作为本地质量门（真实 P2P 依赖公共信令，不适合放进部署流水线）。

## GitHub Pages 部署清单

1. **仓库 Settings → Pages → Build and deployment → Source 必须选 `GitHub Actions`**
   （若选的是「Deploy from a branch」，Pages 会把仓库根目录当站点，直接返回源码版 index.html → 白屏）；
2. 推送到 `main` 或 `master` 会自动触发 `.github/workflows/deploy.yml`（也可手动 Run workflow）；
3. 工作流会跑单元测试 → `pnpm build` → 上传 `dist/` → 发布到 `https://<user>.github.io/<repo>/`；
4. 本地可先自检子路径是否正常（复现线上环境）：

   ```bash
   pnpm build
   node scripts/serve-subpath.mjs turn-based-strategy 4180   # 参数传仓库名，不要传 /repo/
   # 浏览器打开 http://127.0.0.1:4180/turn-based-strategy/
   ```

   Windows/Git Bash 注意：命令行里形如 `/repo/` 的参数会被 MSYS 转换成 `C:/Program Files/Git/repo/`。
